try{
(() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf, __hasOwnProp = Object.prototype.hasOwnProperty;
  var __esm = (fn, res) => function() {
    return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
  };
  var __commonJS = (cb, mod) => function() {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  };
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: !0 });
  }, __copyProps = (to, from, except, desc) => {
    if (from && typeof from == "object" || typeof from == "function")
      for (let key of __getOwnPropNames(from))
        !__hasOwnProp.call(to, key) && key !== except && __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: !0 }) : target,
    mod
  )), __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: !0 }), mod);

  // <define:module>
  var init_define_module = __esm({
    "<define:module>"() {
    }
  });

  // <define:process.env>
  var init_define_process_env = __esm({
    "<define:process.env>"() {
    }
  });

  // <define:process.env.NODE_PATH>
  var init_define_process_env_NODE_PATH = __esm({
    "<define:process.env.NODE_PATH>"() {
    }
  });

  // global-externals:react
  var react_exports = {};
  __export(react_exports, {
    Children: () => Children,
    Component: () => Component,
    Fragment: () => Fragment,
    Profiler: () => Profiler,
    PureComponent: () => PureComponent,
    StrictMode: () => StrictMode,
    Suspense: () => Suspense,
    __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: () => __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED,
    act: () => act,
    cloneElement: () => cloneElement,
    createContext: () => createContext,
    createElement: () => createElement,
    createFactory: () => createFactory,
    createRef: () => createRef,
    default: () => react_default,
    forwardRef: () => forwardRef,
    isValidElement: () => isValidElement,
    lazy: () => lazy,
    memo: () => memo,
    startTransition: () => startTransition,
    unstable_act: () => unstable_act,
    useCallback: () => useCallback,
    useContext: () => useContext,
    useDebugValue: () => useDebugValue,
    useDeferredValue: () => useDeferredValue,
    useEffect: () => useEffect,
    useId: () => useId,
    useImperativeHandle: () => useImperativeHandle,
    useInsertionEffect: () => useInsertionEffect,
    useLayoutEffect: () => useLayoutEffect,
    useMemo: () => useMemo,
    useReducer: () => useReducer,
    useRef: () => useRef,
    useState: () => useState,
    useSyncExternalStore: () => useSyncExternalStore,
    useTransition: () => useTransition,
    version: () => version
  });
  var react_default, Children, Component, Fragment, Profiler, PureComponent, StrictMode, Suspense, __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED, act, cloneElement, createContext, createElement, createFactory, createRef, forwardRef, isValidElement, lazy, memo, startTransition, unstable_act, useCallback, useContext, useDebugValue, useDeferredValue, useEffect, useId, useImperativeHandle, useInsertionEffect, useLayoutEffect, useMemo, useReducer, useRef, useState, useSyncExternalStore, useTransition, version, init_react = __esm({
    "global-externals:react"() {
      init_define_module();
      init_define_process_env();
      init_define_process_env_NODE_PATH();
      react_default = __REACT__, { Children, Component, Fragment, Profiler, PureComponent, StrictMode, Suspense, __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED, act, cloneElement, createContext, createElement, createFactory, createRef, forwardRef, isValidElement, lazy, memo, startTransition, unstable_act, useCallback, useContext, useDebugValue, useDeferredValue, useEffect, useId, useImperativeHandle, useInsertionEffect, useLayoutEffect, useMemo, useReducer, useRef, useState, useSyncExternalStore, useTransition, version } = __REACT__;
    }
  });

  // ../node_modules/react/cjs/react-jsx-runtime.development.js
  var require_react_jsx_runtime_development = __commonJS({
    "../node_modules/react/cjs/react-jsx-runtime.development.js"(exports) {
      "use strict";
      init_define_module();
      init_define_process_env();
      init_define_process_env_NODE_PATH();
      (function() {
        "use strict";
        var React = (init_react(), __toCommonJS(react_exports)), REACT_ELEMENT_TYPE = Symbol.for("react.element"), REACT_PORTAL_TYPE = Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = Symbol.for("react.profiler"), REACT_PROVIDER_TYPE = Symbol.for("react.provider"), REACT_CONTEXT_TYPE = Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"), REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list"), REACT_MEMO_TYPE = Symbol.for("react.memo"), REACT_LAZY_TYPE = Symbol.for("react.lazy"), REACT_OFFSCREEN_TYPE = Symbol.for("react.offscreen"), MAYBE_ITERATOR_SYMBOL = Symbol.iterator, FAUX_ITERATOR_SYMBOL = "@@iterator";
        function getIteratorFn(maybeIterable) {
          if (maybeIterable === null || typeof maybeIterable != "object")
            return null;
          var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
          return typeof maybeIterator == "function" ? maybeIterator : null;
        }
        var ReactSharedInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
        function error(format) {
          {
            for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++)
              args[_key2 - 1] = arguments[_key2];
            printWarning("error", format, args);
          }
        }
        function printWarning(level, format, args) {
          {
            var ReactDebugCurrentFrame2 = ReactSharedInternals.ReactDebugCurrentFrame, stack = ReactDebugCurrentFrame2.getStackAddendum();
            stack !== "" && (format += "%s", args = args.concat([stack]));
            var argsWithFormat = args.map(function(item) {
              return String(item);
            });
            argsWithFormat.unshift("Warning: " + format), Function.prototype.apply.call(console[level], console, argsWithFormat);
          }
        }
        var enableScopeAPI = !1, enableCacheElement = !1, enableTransitionTracing = !1, enableLegacyHidden = !1, enableDebugTracing = !1, REACT_MODULE_REFERENCE;
        REACT_MODULE_REFERENCE = Symbol.for("react.module.reference");
        function isValidElementType(type) {
          return !!(typeof type == "string" || typeof type == "function" || type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || enableDebugTracing || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || enableLegacyHidden || type === REACT_OFFSCREEN_TYPE || enableScopeAPI || enableCacheElement || enableTransitionTracing || typeof type == "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || // This needs to include all possible module reference object
          // types supported by any Flight configuration anywhere since
          // we don't know which Flight build this will end up being used
          // with.
          type.$$typeof === REACT_MODULE_REFERENCE || type.getModuleId !== void 0));
        }
        function getWrappedName(outerType, innerType, wrapperName) {
          var displayName = outerType.displayName;
          if (displayName)
            return displayName;
          var functionName = innerType.displayName || innerType.name || "";
          return functionName !== "" ? wrapperName + "(" + functionName + ")" : wrapperName;
        }
        function getContextName(type) {
          return type.displayName || "Context";
        }
        function getComponentNameFromType(type) {
          if (type == null)
            return null;
          if (typeof type.tag == "number" && error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), typeof type == "function")
            return type.displayName || type.name || null;
          if (typeof type == "string")
            return type;
          switch (type) {
            case REACT_FRAGMENT_TYPE:
              return "Fragment";
            case REACT_PORTAL_TYPE:
              return "Portal";
            case REACT_PROFILER_TYPE:
              return "Profiler";
            case REACT_STRICT_MODE_TYPE:
              return "StrictMode";
            case REACT_SUSPENSE_TYPE:
              return "Suspense";
            case REACT_SUSPENSE_LIST_TYPE:
              return "SuspenseList";
          }
          if (typeof type == "object")
            switch (type.$$typeof) {
              case REACT_CONTEXT_TYPE:
                var context = type;
                return getContextName(context) + ".Consumer";
              case REACT_PROVIDER_TYPE:
                var provider = type;
                return getContextName(provider._context) + ".Provider";
              case REACT_FORWARD_REF_TYPE:
                return getWrappedName(type, type.render, "ForwardRef");
              case REACT_MEMO_TYPE:
                var outerName = type.displayName || null;
                return outerName !== null ? outerName : getComponentNameFromType(type.type) || "Memo";
              case REACT_LAZY_TYPE: {
                var lazyComponent = type, payload = lazyComponent._payload, init = lazyComponent._init;
                try {
                  return getComponentNameFromType(init(payload));
                } catch {
                  return null;
                }
              }
            }
          return null;
        }
        var assign = Object.assign, disabledDepth = 0, prevLog, prevInfo, prevWarn, prevError, prevGroup, prevGroupCollapsed, prevGroupEnd;
        function disabledLog() {
        }
        disabledLog.__reactDisabledLog = !0;
        function disableLogs() {
          {
            if (disabledDepth === 0) {
              prevLog = console.log, prevInfo = console.info, prevWarn = console.warn, prevError = console.error, prevGroup = console.group, prevGroupCollapsed = console.groupCollapsed, prevGroupEnd = console.groupEnd;
              var props = {
                configurable: !0,
                enumerable: !0,
                value: disabledLog,
                writable: !0
              };
              Object.defineProperties(console, {
                info: props,
                log: props,
                warn: props,
                error: props,
                group: props,
                groupCollapsed: props,
                groupEnd: props
              });
            }
            disabledDepth++;
          }
        }
        function reenableLogs() {
          {
            if (disabledDepth--, disabledDepth === 0) {
              var props = {
                configurable: !0,
                enumerable: !0,
                writable: !0
              };
              Object.defineProperties(console, {
                log: assign({}, props, {
                  value: prevLog
                }),
                info: assign({}, props, {
                  value: prevInfo
                }),
                warn: assign({}, props, {
                  value: prevWarn
                }),
                error: assign({}, props, {
                  value: prevError
                }),
                group: assign({}, props, {
                  value: prevGroup
                }),
                groupCollapsed: assign({}, props, {
                  value: prevGroupCollapsed
                }),
                groupEnd: assign({}, props, {
                  value: prevGroupEnd
                })
              });
            }
            disabledDepth < 0 && error("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
          }
        }
        var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher, prefix;
        function describeBuiltInComponentFrame(name, source, ownerFn) {
          {
            if (prefix === void 0)
              try {
                throw Error();
              } catch (x) {
                var match = x.stack.trim().match(/\n( *(at )?)/);
                prefix = match && match[1] || "";
              }
            return `
` + prefix + name;
          }
        }
        var reentry = !1, componentFrameCache;
        {
          var PossiblyWeakMap = typeof WeakMap == "function" ? WeakMap : Map;
          componentFrameCache = new PossiblyWeakMap();
        }
        function describeNativeComponentFrame(fn, construct) {
          if (!fn || reentry)
            return "";
          {
            var frame = componentFrameCache.get(fn);
            if (frame !== void 0)
              return frame;
          }
          var control;
          reentry = !0;
          var previousPrepareStackTrace = Error.prepareStackTrace;
          Error.prepareStackTrace = void 0;
          var previousDispatcher;
          previousDispatcher = ReactCurrentDispatcher.current, ReactCurrentDispatcher.current = null, disableLogs();
          try {
            if (construct) {
              var Fake = function() {
                throw Error();
              };
              if (Object.defineProperty(Fake.prototype, "props", {
                set: function() {
                  throw Error();
                }
              }), typeof Reflect == "object" && Reflect.construct) {
                try {
                  Reflect.construct(Fake, []);
                } catch (x) {
                  control = x;
                }
                Reflect.construct(fn, [], Fake);
              } else {
                try {
                  Fake.call();
                } catch (x) {
                  control = x;
                }
                fn.call(Fake.prototype);
              }
            } else {
              try {
                throw Error();
              } catch (x) {
                control = x;
              }
              fn();
            }
          } catch (sample) {
            if (sample && control && typeof sample.stack == "string") {
              for (var sampleLines = sample.stack.split(`
`), controlLines = control.stack.split(`
`), s = sampleLines.length - 1, c = controlLines.length - 1; s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c]; )
                c--;
              for (; s >= 1 && c >= 0; s--, c--)
                if (sampleLines[s] !== controlLines[c]) {
                  if (s !== 1 || c !== 1)
                    do
                      if (s--, c--, c < 0 || sampleLines[s] !== controlLines[c]) {
                        var _frame = `
` + sampleLines[s].replace(" at new ", " at ");
                        return fn.displayName && _frame.includes("<anonymous>") && (_frame = _frame.replace("<anonymous>", fn.displayName)), typeof fn == "function" && componentFrameCache.set(fn, _frame), _frame;
                      }
                    while (s >= 1 && c >= 0);
                  break;
                }
            }
          } finally {
            reentry = !1, ReactCurrentDispatcher.current = previousDispatcher, reenableLogs(), Error.prepareStackTrace = previousPrepareStackTrace;
          }
          var name = fn ? fn.displayName || fn.name : "", syntheticFrame = name ? describeBuiltInComponentFrame(name) : "";
          return typeof fn == "function" && componentFrameCache.set(fn, syntheticFrame), syntheticFrame;
        }
        function describeFunctionComponentFrame(fn, source, ownerFn) {
          return describeNativeComponentFrame(fn, !1);
        }
        function shouldConstruct(Component2) {
          var prototype = Component2.prototype;
          return !!(prototype && prototype.isReactComponent);
        }
        function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
          if (type == null)
            return "";
          if (typeof type == "function")
            return describeNativeComponentFrame(type, shouldConstruct(type));
          if (typeof type == "string")
            return describeBuiltInComponentFrame(type);
          switch (type) {
            case REACT_SUSPENSE_TYPE:
              return describeBuiltInComponentFrame("Suspense");
            case REACT_SUSPENSE_LIST_TYPE:
              return describeBuiltInComponentFrame("SuspenseList");
          }
          if (typeof type == "object")
            switch (type.$$typeof) {
              case REACT_FORWARD_REF_TYPE:
                return describeFunctionComponentFrame(type.render);
              case REACT_MEMO_TYPE:
                return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
              case REACT_LAZY_TYPE: {
                var lazyComponent = type, payload = lazyComponent._payload, init = lazyComponent._init;
                try {
                  return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
                } catch {
                }
              }
            }
          return "";
        }
        var hasOwnProperty = Object.prototype.hasOwnProperty, loggedTypeFailures = {}, ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
        function setCurrentlyValidatingElement(element) {
          if (element) {
            var owner = element._owner, stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
            ReactDebugCurrentFrame.setExtraStackFrame(stack);
          } else
            ReactDebugCurrentFrame.setExtraStackFrame(null);
        }
        function checkPropTypes(typeSpecs, values, location, componentName, element) {
          {
            var has = Function.call.bind(hasOwnProperty);
            for (var typeSpecName in typeSpecs)
              if (has(typeSpecs, typeSpecName)) {
                var error$1 = void 0;
                try {
                  if (typeof typeSpecs[typeSpecName] != "function") {
                    var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                    throw err.name = "Invariant Violation", err;
                  }
                  error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
                } catch (ex) {
                  error$1 = ex;
                }
                error$1 && !(error$1 instanceof Error) && (setCurrentlyValidatingElement(element), error("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || "React class", location, typeSpecName, typeof error$1), setCurrentlyValidatingElement(null)), error$1 instanceof Error && !(error$1.message in loggedTypeFailures) && (loggedTypeFailures[error$1.message] = !0, setCurrentlyValidatingElement(element), error("Failed %s type: %s", location, error$1.message), setCurrentlyValidatingElement(null));
              }
          }
        }
        var isArrayImpl = Array.isArray;
        function isArray(a) {
          return isArrayImpl(a);
        }
        function typeName(value) {
          {
            var hasToStringTag = typeof Symbol == "function" && Symbol.toStringTag, type = hasToStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
            return type;
          }
        }
        function willCoercionThrow(value) {
          try {
            return testStringCoercion(value), !1;
          } catch {
            return !0;
          }
        }
        function testStringCoercion(value) {
          return "" + value;
        }
        function checkKeyStringCoercion(value) {
          if (willCoercionThrow(value))
            return error("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value)), testStringCoercion(value);
        }
        var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner, RESERVED_PROPS = {
          key: !0,
          ref: !0,
          __self: !0,
          __source: !0
        }, specialPropKeyWarningShown, specialPropRefWarningShown, didWarnAboutStringRefs;
        didWarnAboutStringRefs = {};
        function hasValidRef(config) {
          if (hasOwnProperty.call(config, "ref")) {
            var getter = Object.getOwnPropertyDescriptor(config, "ref").get;
            if (getter && getter.isReactWarning)
              return !1;
          }
          return config.ref !== void 0;
        }
        function hasValidKey(config) {
          if (hasOwnProperty.call(config, "key")) {
            var getter = Object.getOwnPropertyDescriptor(config, "key").get;
            if (getter && getter.isReactWarning)
              return !1;
          }
          return config.key !== void 0;
        }
        function warnIfStringRefCannotBeAutoConverted(config, self) {
          if (typeof config.ref == "string" && ReactCurrentOwner.current && self && ReactCurrentOwner.current.stateNode !== self) {
            var componentName = getComponentNameFromType(ReactCurrentOwner.current.type);
            didWarnAboutStringRefs[componentName] || (error('Component "%s" contains the string ref "%s". Support for string refs will be removed in a future major release. This case cannot be automatically converted to an arrow function. We ask you to manually fix this case by using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref', getComponentNameFromType(ReactCurrentOwner.current.type), config.ref), didWarnAboutStringRefs[componentName] = !0);
          }
        }
        function defineKeyPropWarningGetter(props, displayName) {
          {
            var warnAboutAccessingKey = function() {
              specialPropKeyWarningShown || (specialPropKeyWarningShown = !0, error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName));
            };
            warnAboutAccessingKey.isReactWarning = !0, Object.defineProperty(props, "key", {
              get: warnAboutAccessingKey,
              configurable: !0
            });
          }
        }
        function defineRefPropWarningGetter(props, displayName) {
          {
            var warnAboutAccessingRef = function() {
              specialPropRefWarningShown || (specialPropRefWarningShown = !0, error("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName));
            };
            warnAboutAccessingRef.isReactWarning = !0, Object.defineProperty(props, "ref", {
              get: warnAboutAccessingRef,
              configurable: !0
            });
          }
        }
        var ReactElement = function(type, key, ref, self, source, owner, props) {
          var element = {
            // This tag allows us to uniquely identify this as a React Element
            $$typeof: REACT_ELEMENT_TYPE,
            // Built-in properties that belong on the element
            type,
            key,
            ref,
            props,
            // Record the component responsible for creating this element.
            _owner: owner
          };
          return element._store = {}, Object.defineProperty(element._store, "validated", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: !1
          }), Object.defineProperty(element, "_self", {
            configurable: !1,
            enumerable: !1,
            writable: !1,
            value: self
          }), Object.defineProperty(element, "_source", {
            configurable: !1,
            enumerable: !1,
            writable: !1,
            value: source
          }), Object.freeze && (Object.freeze(element.props), Object.freeze(element)), element;
        };
        function jsxDEV(type, config, maybeKey, source, self) {
          {
            var propName, props = {}, key = null, ref = null;
            maybeKey !== void 0 && (checkKeyStringCoercion(maybeKey), key = "" + maybeKey), hasValidKey(config) && (checkKeyStringCoercion(config.key), key = "" + config.key), hasValidRef(config) && (ref = config.ref, warnIfStringRefCannotBeAutoConverted(config, self));
            for (propName in config)
              hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName) && (props[propName] = config[propName]);
            if (type && type.defaultProps) {
              var defaultProps = type.defaultProps;
              for (propName in defaultProps)
                props[propName] === void 0 && (props[propName] = defaultProps[propName]);
            }
            if (key || ref) {
              var displayName = typeof type == "function" ? type.displayName || type.name || "Unknown" : type;
              key && defineKeyPropWarningGetter(props, displayName), ref && defineRefPropWarningGetter(props, displayName);
            }
            return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
          }
        }
        var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner, ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
        function setCurrentlyValidatingElement$1(element) {
          if (element) {
            var owner = element._owner, stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
            ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
          } else
            ReactDebugCurrentFrame$1.setExtraStackFrame(null);
        }
        var propTypesMisspellWarningShown;
        propTypesMisspellWarningShown = !1;
        function isValidElement2(object) {
          return typeof object == "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
        }
        function getDeclarationErrorAddendum() {
          {
            if (ReactCurrentOwner$1.current) {
              var name = getComponentNameFromType(ReactCurrentOwner$1.current.type);
              if (name)
                return `

Check the render method of \`` + name + "`.";
            }
            return "";
          }
        }
        function getSourceInfoErrorAddendum(source) {
          {
            if (source !== void 0) {
              var fileName = source.fileName.replace(/^.*[\\\/]/, ""), lineNumber = source.lineNumber;
              return `

Check your code at ` + fileName + ":" + lineNumber + ".";
            }
            return "";
          }
        }
        var ownerHasKeyUseWarning = {};
        function getCurrentComponentErrorInfo(parentType) {
          {
            var info = getDeclarationErrorAddendum();
            if (!info) {
              var parentName = typeof parentType == "string" ? parentType : parentType.displayName || parentType.name;
              parentName && (info = `

Check the top-level render call using <` + parentName + ">.");
            }
            return info;
          }
        }
        function validateExplicitKey(element, parentType) {
          {
            if (!element._store || element._store.validated || element.key != null)
              return;
            element._store.validated = !0;
            var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
            if (ownerHasKeyUseWarning[currentComponentErrorInfo])
              return;
            ownerHasKeyUseWarning[currentComponentErrorInfo] = !0;
            var childOwner = "";
            element && element._owner && element._owner !== ReactCurrentOwner$1.current && (childOwner = " It was passed a child from " + getComponentNameFromType(element._owner.type) + "."), setCurrentlyValidatingElement$1(element), error('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', currentComponentErrorInfo, childOwner), setCurrentlyValidatingElement$1(null);
          }
        }
        function validateChildKeys(node, parentType) {
          {
            if (typeof node != "object")
              return;
            if (isArray(node))
              for (var i = 0; i < node.length; i++) {
                var child = node[i];
                isValidElement2(child) && validateExplicitKey(child, parentType);
              }
            else if (isValidElement2(node))
              node._store && (node._store.validated = !0);
            else if (node) {
              var iteratorFn = getIteratorFn(node);
              if (typeof iteratorFn == "function" && iteratorFn !== node.entries)
                for (var iterator = iteratorFn.call(node), step; !(step = iterator.next()).done; )
                  isValidElement2(step.value) && validateExplicitKey(step.value, parentType);
            }
          }
        }
        function validatePropTypes(element) {
          {
            var type = element.type;
            if (type == null || typeof type == "string")
              return;
            var propTypes;
            if (typeof type == "function")
              propTypes = type.propTypes;
            else if (typeof type == "object" && (type.$$typeof === REACT_FORWARD_REF_TYPE || // Note: Memo only checks outer props here.
            // Inner props are checked in the reconciler.
            type.$$typeof === REACT_MEMO_TYPE))
              propTypes = type.propTypes;
            else
              return;
            if (propTypes) {
              var name = getComponentNameFromType(type);
              checkPropTypes(propTypes, element.props, "prop", name, element);
            } else if (type.PropTypes !== void 0 && !propTypesMisspellWarningShown) {
              propTypesMisspellWarningShown = !0;
              var _name = getComponentNameFromType(type);
              error("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", _name || "Unknown");
            }
            typeof type.getDefaultProps == "function" && !type.getDefaultProps.isReactClassApproved && error("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
          }
        }
        function validateFragmentProps(fragment) {
          {
            for (var keys = Object.keys(fragment.props), i = 0; i < keys.length; i++) {
              var key = keys[i];
              if (key !== "children" && key !== "key") {
                setCurrentlyValidatingElement$1(fragment), error("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", key), setCurrentlyValidatingElement$1(null);
                break;
              }
            }
            fragment.ref !== null && (setCurrentlyValidatingElement$1(fragment), error("Invalid attribute `ref` supplied to `React.Fragment`."), setCurrentlyValidatingElement$1(null));
          }
        }
        var didWarnAboutKeySpread = {};
        function jsxWithValidation(type, props, key, isStaticChildren, source, self) {
          {
            var validType = isValidElementType(type);
            if (!validType) {
              var info = "";
              (type === void 0 || typeof type == "object" && type !== null && Object.keys(type).length === 0) && (info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.");
              var sourceInfo = getSourceInfoErrorAddendum(source);
              sourceInfo ? info += sourceInfo : info += getDeclarationErrorAddendum();
              var typeString;
              type === null ? typeString = "null" : isArray(type) ? typeString = "array" : type !== void 0 && type.$$typeof === REACT_ELEMENT_TYPE ? (typeString = "<" + (getComponentNameFromType(type.type) || "Unknown") + " />", info = " Did you accidentally export a JSX literal instead of a component?") : typeString = typeof type, error("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", typeString, info);
            }
            var element = jsxDEV(type, props, key, source, self);
            if (element == null)
              return element;
            if (validType) {
              var children = props.children;
              if (children !== void 0)
                if (isStaticChildren)
                  if (isArray(children)) {
                    for (var i = 0; i < children.length; i++)
                      validateChildKeys(children[i], type);
                    Object.freeze && Object.freeze(children);
                  } else
                    error("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
                else
                  validateChildKeys(children, type);
            }
            if (hasOwnProperty.call(props, "key")) {
              var componentName = getComponentNameFromType(type), keys = Object.keys(props).filter(function(k) {
                return k !== "key";
              }), beforeExample = keys.length > 0 ? "{key: someKey, " + keys.join(": ..., ") + ": ...}" : "{key: someKey}";
              if (!didWarnAboutKeySpread[componentName + beforeExample]) {
                var afterExample = keys.length > 0 ? "{" + keys.join(": ..., ") + ": ...}" : "{}";
                error(`A props object containing a "key" prop is being spread into JSX:
  let props = %s;
  <%s {...props} />
React keys must be passed directly to JSX without using spread:
  let props = %s;
  <%s key={someKey} {...props} />`, beforeExample, componentName, afterExample, componentName), didWarnAboutKeySpread[componentName + beforeExample] = !0;
              }
            }
            return type === REACT_FRAGMENT_TYPE ? validateFragmentProps(element) : validatePropTypes(element), element;
          }
        }
        function jsxWithValidationStatic(type, props, key) {
          return jsxWithValidation(type, props, key, !0);
        }
        function jsxWithValidationDynamic(type, props, key) {
          return jsxWithValidation(type, props, key, !1);
        }
        var jsx8 = jsxWithValidationDynamic, jsxs6 = jsxWithValidationStatic;
        exports.Fragment = REACT_FRAGMENT_TYPE, exports.jsx = jsx8, exports.jsxs = jsxs6;
      })();
    }
  });

  // ../node_modules/react/jsx-runtime.js
  var require_jsx_runtime = __commonJS({
    "../node_modules/react/jsx-runtime.js"(exports, module) {
      "use strict";
      init_define_module();
      init_define_process_env();
      init_define_process_env_NODE_PATH();
      module.exports = require_react_jsx_runtime_development();
    }
  });

  // node_modules/.cache/storybook/10.4.0/f4477d8d8c64b28dfca6b1bd1805847016474df766382699f5f5ccfdd2ae2acb/sb-manager/addon-1/manager-bundle.js
  init_define_module();
  init_define_process_env();
  init_define_process_env_NODE_PATH();

  // ../addon/dist/manager.mjs
  init_define_module();
  init_define_process_env();
  init_define_process_env_NODE_PATH();

  // global-externals:storybook/manager-api
  init_define_module();
  init_define_process_env();
  init_define_process_env_NODE_PATH();
  var manager_api_default = __STORYBOOK_API__, { ActiveTabs, Consumer, ManagerContext, Provider, RequestResponseError, Tag, addons, combineParameters, controlOrMetaKey, controlOrMetaSymbol, eventMatchesShortcut, eventToShortcut, experimental_MockUniversalStore, experimental_UniversalStore, experimental_getStatusStore, experimental_getTestProviderStore, experimental_requestResponse, experimental_useStatusStore, experimental_useTestProviderStore, experimental_useUniversalStore, internal_checklistStore, internal_fullStatusStore, internal_fullTestProviderStore, internal_universalChecklistStore, internal_universalStatusStore, internal_universalTestProviderStore, isMacLike, isShortcutTaken, keyToSymbol, merge, mockChannel, optionOrAltSymbol, shortcutMatchesShortcut, shortcutToAriaKeyshortcuts, shortcutToHumanString, types, useAddonState, useArgTypes, useArgs, useChannel, useGlobalTypes, useGlobals, useParameter, useSharedState, useStoryPrepared, useStorybookApi, useStorybookState } = __STORYBOOK_API__;

  // global-externals:storybook/internal/components
  init_define_module();
  init_define_process_env();
  init_define_process_env_NODE_PATH();
  var components_default = __STORYBOOK_COMPONENTS__, { A, AbstractToolbar, ActionBar, ActionList, AddonPanel, Badge, Bar, Blockquote, Button, Card, ClipboardCode, Code, Collapsible, DL, Div, DocumentWrapper, EmptyTabContent, ErrorFormatter, FlexBar, Form, H1, H2, H3, H4, H5, H6, HR, IconButton, Img, LI, Link, ListItem, Loader, Modal, ModalDecorator, OL, P, Placeholder, Popover, PopoverProvider, Pre, ProgressSpinner, ResetWrapper, ScrollArea, Select, Separator, Spaced, Span, StatelessTab, StatelessTabList, StatelessTabPanel, StatelessTabsView, StorybookIcon, StorybookLogo, SyntaxHighlighter, TT, TabBar, TabButton, TabList, TabPanel, TabWrapper, Table, Tabs, TabsState, TabsView, ToggleButton, Toolbar, Tooltip, TooltipLinkList, TooltipMessage, TooltipNote, TooltipProvider, UL, WithTooltip, WithTooltipPure, Zoom, codeCommon, components, convertToReactAriaPlacement, createCopyToClipboardFunction, getStoryHref, interleaveSeparators, nameSpaceClassNames, resetComponents, useTabsState, withReset } = __STORYBOOK_COMPONENTS__;

  // global-externals:@storybook/icons
  init_define_module();
  init_define_process_env();
  init_define_process_env_NODE_PATH();
  var icons_default = __STORYBOOK_ICONS__, { AccessibilityAltIcon, AccessibilityIcon, AccessibilityIgnoredIcon, AddIcon, AdminIcon, AlertAltIcon, AlertIcon, AlignLeftIcon, AlignRightIcon, AppleIcon, ArrowBottomLeftIcon, ArrowBottomRightIcon, ArrowDownIcon, ArrowLeftIcon, ArrowRightIcon, ArrowSolidDownIcon, ArrowSolidLeftIcon, ArrowSolidRightIcon, ArrowSolidUpIcon, ArrowTopLeftIcon, ArrowTopRightIcon, ArrowUpIcon, AzureDevOpsIcon, BackIcon, BasketIcon, BatchAcceptIcon, BatchDenyIcon, BeakerIcon, BellIcon, BitbucketIcon, BoldIcon, BookIcon, BookmarkHollowIcon, BookmarkIcon, BottomBarIcon, BottomBarToggleIcon, BoxIcon, BranchIcon, BrowserIcon, BugIcon, ButtonIcon, CPUIcon, CalendarIcon, CameraIcon, CameraStabilizeIcon, CategoryIcon, CertificateIcon, ChangedIcon, ChatIcon, CheckIcon, ChecklistIcon, ChevronDownIcon, ChevronLeftIcon, ChevronRightIcon, ChevronSmallDownIcon, ChevronSmallLeftIcon, ChevronSmallRightIcon, ChevronSmallUpIcon, ChevronUpIcon, ChromaticIcon, ChromeIcon, CircleHollowIcon, CircleIcon, ClearIcon, CloseAltIcon, CloseIcon, CloudHollowIcon, CloudIcon, CogIcon, CollapseIcon, CommandIcon, CommentAddIcon, CommentIcon, CommentsIcon, CommitIcon, CompassIcon, ComponentDrivenIcon, ComponentIcon, ContrastIcon, ContrastIgnoredIcon, ControlsIcon, CopyIcon, CreditIcon, CrossIcon, DashboardIcon, DatabaseIcon, DeleteIcon, DiamondIcon, DirectionIcon, DiscordIcon, DocChartIcon, DocListIcon, DocumentIcon, DownloadIcon, DragIcon, EditIcon, EditorIcon, EllipsisIcon, EmailIcon, ExpandAltIcon, ExpandIcon, EyeCloseIcon, EyeIcon, FaceHappyIcon, FaceNeutralIcon, FaceSadIcon, FacebookIcon, FailedIcon, FastForwardIcon, FigmaIcon, FilesIcon, FilterIcon, FlagIcon, FolderIcon, FormIcon, GDriveIcon, GiftIcon, GithubIcon, GitlabIcon, GlobeIcon, GoogleIcon, GraphBarIcon, GraphLineIcon, GraphqlIcon, GridAltIcon, GridIcon, GrowIcon, HeartHollowIcon, HeartIcon, HomeIcon, HourglassIcon, InfoIcon, ItalicIcon, JumpToIcon, KeyIcon, LightningIcon, LightningOffIcon, LinkBrokenIcon, LinkIcon, LinkedinIcon, LinuxIcon, ListOrderedIcon, ListUnorderedIcon, LocationIcon, LockIcon, MarkdownIcon, MarkupIcon, MediumIcon, MemoryIcon, MenuIcon, MergeIcon, MirrorIcon, MobileIcon, MoonIcon, NutIcon, OutboxIcon, OutlineIcon, PaintBrushAltIcon, PaintBrushIcon, PaperClipIcon, ParagraphIcon, PassedIcon, PhoneIcon, PhotoDragIcon, PhotoIcon, PhotoStabilizeIcon, PinAltIcon, PinIcon, PlayAllHollowIcon, PlayBackIcon, PlayHollowIcon, PlayIcon, PlayNextIcon, PlusIcon, PointerDefaultIcon, PointerHandIcon, PopOutIcon, PowerIcon, PrintIcon, ProceedIcon, ProfileIcon, PullRequestIcon, QuestionIcon, RSSIcon, RedirectIcon, ReduxIcon, RefreshIcon, ReplyIcon, RepoIcon, RequestChangeIcon, RewindIcon, RulerIcon, SaveIcon, SearchIcon, ShareAltIcon, ShareIcon, ShieldIcon, SideBySideIcon, SidebarAltIcon, SidebarAltToggleIcon, SidebarIcon, SidebarToggleIcon, SortDownIcon, SortUpIcon, SpeakerIcon, StackedIcon, StarHollowIcon, StarIcon, StatusFailIcon, StatusIcon, StatusNewIcon, StatusPassIcon, StatusWarnIcon, StickerIcon, StopAltHollowIcon, StopAltIcon, StopIcon, StorybookIcon: StorybookIcon2, StructureIcon, SubtractIcon, SunIcon, SupportIcon, SweepIcon, SwitchAltIcon, SyncIcon, TabletIcon, ThumbsUpIcon, TimeIcon, TimerIcon, TransferIcon, TrashIcon, TwitterIcon, TypeIcon, UbuntuIcon, UndoIcon, UnfoldIcon, UnlockIcon, UnpinIcon, UploadIcon, UserAddIcon, UserAltIcon, UserIcon, UsersIcon, VSCodeIcon, VerifiedIcon, VideoIcon, WandIcon, WatchIcon, WindowsIcon, WrenchIcon, XIcon, YoutubeIcon, ZoomIcon, ZoomOutIcon, ZoomResetIcon, iconList } = __STORYBOOK_ICONS__;

  // global-externals:storybook/theming
  init_define_module();
  init_define_process_env();
  init_define_process_env_NODE_PATH();
  var theming_default = __STORYBOOK_THEMING__, { CacheProvider, ClassNames, Global, ThemeProvider, background, color, convert, create, createCache, createGlobal, createReset, css, darken, ensure, getPreferredColorScheme, ignoreSsrWarning, isPropValid, jsx, keyframes, lighten, srOnlyStyles, styled, themes, tokens, typography, useTheme, withTheme } = __STORYBOOK_THEMING__;

  // ../addon/dist/manager.mjs
  init_react();
  init_react();
  init_react();
  var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
  var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
  var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
  var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
  init_react();
  var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1), import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
  init_react();
  var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1), StarsContainer = styled.div({
    display: "flex",
    alignItems: "center",
    gap: "6px",
    padding: "4px 0"
  }), StarButton = styled.button(({ theme, active, hoverActive }) => ({
    background: "none",
    border: "none",
    cursor: "pointer",
    padding: 0,
    outline: "none",
    color: hoverActive || active ? "#FFB800" : theme.appBorderColor || "#DDDDDD",
    transition: "transform 0.1s, color 0.1s",
    "&:hover": {
      transform: "scale(1.15)"
    },
    "&:active": {
      transform: "scale(0.95)"
    }
  })), StarIcon2 = () => (0, import_jsx_runtime.jsx)("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "currentColor", children: (0, import_jsx_runtime.jsx)("path", { d: "M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z" }) }), StarRatingInput = ({ id, value, onChange }) => {
    let [hoverRating, setHoverRating] = useState(0);
    return (0, import_jsx_runtime.jsx)(StarsContainer, { id, children: [1, 2, 3, 4, 5].map((starValue) => {
      let active = value >= starValue, hoverActive = hoverRating >= starValue;
      return (0, import_jsx_runtime.jsx)(
        StarButton,
        {
          type: "button",
          active,
          hoverActive,
          onMouseEnter: () => setHoverRating(starValue),
          onMouseLeave: () => setHoverRating(0),
          onClick: () => onChange(starValue),
          children: (0, import_jsx_runtime.jsx)(StarIcon2, {})
        },
        starValue
      );
    }) });
  }, SelectionGroup = styled.div({
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    padding: "4px 0"
  }), SelectionLabel = styled.label(({ theme }) => ({
    display: "flex",
    alignItems: "center",
    gap: "8px",
    fontSize: "13px",
    color: theme.textColor,
    cursor: "pointer",
    userSelect: "none"
  })), RadioInput = styled.input(({ theme }) => ({
    cursor: "pointer",
    accentColor: theme.color.secondary
  })), RadioGroupInput = ({ name, options, value, onChange }) => (0, import_jsx_runtime2.jsx)(SelectionGroup, { children: options.map((option) => (0, import_jsx_runtime2.jsxs)(SelectionLabel, { children: [
    (0, import_jsx_runtime2.jsx)(
      RadioInput,
      {
        type: "radio",
        name,
        checked: value === option,
        onChange: () => onChange(option)
      }
    ),
    option
  ] }, option)) }), SelectionGroup2 = styled.div({
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    padding: "4px 0"
  }), SelectionLabel2 = styled.label(({ theme }) => ({
    display: "flex",
    alignItems: "center",
    gap: "8px",
    fontSize: "13px",
    color: theme.textColor,
    cursor: "pointer",
    userSelect: "none"
  })), CheckboxInput = styled.input(({ theme }) => ({
    cursor: "pointer",
    accentColor: theme.color.secondary
  })), CheckboxGroupInput = ({ options, values = [], onChange }) => (0, import_jsx_runtime3.jsx)(SelectionGroup2, { children: options.map((option) => {
    let isChecked = values.includes(option);
    return (0, import_jsx_runtime3.jsxs)(SelectionLabel2, { children: [
      (0, import_jsx_runtime3.jsx)(
        CheckboxInput,
        {
          type: "checkbox",
          checked: isChecked,
          onChange: (e) => onChange(option, e.target.checked)
        }
      ),
      option
    ] }, option);
  }) }), InputText = styled.input(({ theme }) => ({
    padding: "8px 12px",
    fontSize: "13px",
    borderRadius: "4px",
    border: `1px solid ${theme.appBorderColor}`,
    backgroundColor: theme.background.app,
    color: theme.textColor,
    outline: "none",
    transition: "border-color 0.2s",
    "&:focus": {
      borderColor: theme.color.secondary
    }
  })), TextArea = styled.textarea(({ theme }) => ({
    padding: "8px 12px",
    fontSize: "13px",
    borderRadius: "4px",
    border: `1px solid ${theme.appBorderColor}`,
    backgroundColor: theme.background.app,
    color: theme.color.primary,
    outline: "none",
    resize: "vertical",
    minHeight: "80px",
    transition: "border-color 0.2s",
    "&:focus": {
      borderColor: theme.color.secondary
    }
  })), TextInputField = ({ id, placeholder, value, onChange }) => (0, import_jsx_runtime4.jsx)(
    InputText,
    {
      type: "text",
      id,
      placeholder,
      value,
      onChange: (e) => onChange(e.target.value)
    }
  ), TextAreaField = ({ id, placeholder, value, onChange }) => (0, import_jsx_runtime4.jsx)(
    TextArea,
    {
      id,
      placeholder,
      value,
      onChange: (e) => onChange(e.target.value)
    }
  ), useSurveyStorage = (surveyId) => {
    let completedKey = `sb-survey-completed-${surveyId}`, skippedPermanentlyKey = `sb-survey-skipped-${surveyId}`, dismissedAtKey = `sb-survey-dismissed-at-${surveyId}`, impressionCountKey = `sb-survey-impressions-${surveyId}`, draftKey = `sb-survey-draft-${surveyId}`, [state, setState] = useState(() => {
      try {
        let isCompleted = localStorage.getItem(completedKey) === "true", isSkippedPermanently = localStorage.getItem(skippedPermanentlyKey) === "true", dismissed = localStorage.getItem(dismissedAtKey), dismissedAt = dismissed ? Number(dismissed) : null, impressionCount = parseInt(localStorage.getItem(impressionCountKey) || "0", 10);
        return {
          isCompleted,
          isSkippedPermanently,
          dismissedAt,
          impressionCount
        };
      } catch {
        return {
          isCompleted: !1,
          isSkippedPermanently: !1,
          dismissedAt: null,
          impressionCount: 0
        };
      }
    });
    useEffect(() => {
      try {
        let isCompleted = localStorage.getItem(completedKey) === "true", isSkippedPermanently = localStorage.getItem(skippedPermanentlyKey) === "true", dismissed = localStorage.getItem(dismissedAtKey), dismissedAt = dismissed ? Number(dismissed) : null, impressionCount = parseInt(localStorage.getItem(impressionCountKey) || "0", 10);
        setState({
          isCompleted,
          isSkippedPermanently,
          dismissedAt,
          impressionCount
        });
      } catch {
      }
    }, [completedKey, skippedPermanentlyKey, dismissedAtKey, impressionCountKey]);
    let setCompleted = useCallback(() => {
      try {
        localStorage.setItem(completedKey, "true"), setState((prev) => ({ ...prev, isCompleted: !0 }));
      } catch (e) {
        console.error("[Feedback Survey Storage] Failed to set completed state:", e);
      }
    }, [completedKey]), setSkippedPermanently = useCallback(() => {
      try {
        localStorage.setItem(skippedPermanentlyKey, "true"), setState((prev) => ({ ...prev, isSkippedPermanently: !0 }));
      } catch (e) {
        console.error("[Feedback Survey Storage] Failed to set skippedPermanently state:", e);
      }
    }, [skippedPermanentlyKey]), setDismissed = useCallback((timestamp) => {
      try {
        localStorage.setItem(dismissedAtKey, String(timestamp)), setState((prev) => ({ ...prev, dismissedAt: timestamp }));
      } catch (e) {
        console.error("[Feedback Survey Storage] Failed to set dismissed state:", e);
      }
    }, [dismissedAtKey]), incrementImpressions = useCallback(() => {
      let nextCount = 0;
      try {
        nextCount = parseInt(localStorage.getItem(impressionCountKey) || "0", 10) + 1, localStorage.setItem(impressionCountKey, String(nextCount)), setState((prev) => ({ ...prev, impressionCount: nextCount }));
      } catch (e) {
        console.error("[Feedback Survey Storage] Failed to increment impression count:", e);
      }
      return nextCount;
    }, [impressionCountKey]), getDraft = useCallback(() => {
      try {
        let draft = sessionStorage.getItem(draftKey);
        return draft ? JSON.parse(draft) : {};
      } catch {
        return {};
      }
    }, [draftKey]), saveDraft = useCallback((values) => {
      try {
        sessionStorage.setItem(draftKey, JSON.stringify(values));
      } catch (e) {
        console.error("[Feedback Survey Storage] Failed to save draft values:", e);
      }
    }, [draftKey]), clearDraft = useCallback(() => {
      try {
        sessionStorage.removeItem(draftKey);
      } catch (e) {
        console.error("[Feedback Survey Storage] Failed to clear draft values:", e);
      }
    }, [draftKey]);
    return {
      ...state,
      setCompleted,
      setSkippedPermanently,
      setDismissed,
      incrementImpressions,
      getDraft,
      saveDraft,
      clearDraft
    };
  }, FormContainer = styled.form({
    display: "flex",
    flexDirection: "column",
    gap: "16px",
    fontFamily: '"Nunito Sans", -apple-system, sans-serif'
  }), FormGroup = styled.div({
    display: "flex",
    flexDirection: "column",
    gap: "6px"
  }), Label = styled.label(({ theme }) => ({
    fontSize: "13px",
    fontWeight: "700",
    color: theme.textColor,
    display: "flex",
    alignItems: "center",
    gap: "4px"
  })), RequiredAsterisk = styled.span(({ theme }) => ({
    color: theme.color.negative
  })), ErrorText = styled.span(({ theme }) => ({
    fontSize: "11px",
    color: theme.color.negative,
    marginTop: "2px"
  })), ButtonContainer = styled.div({
    display: "flex",
    justifyContent: "flex-end",
    gap: "12px",
    marginTop: "8px"
  }), SubmitButton = styled.button(({ theme, disabled }) => ({
    padding: "8px 16px",
    fontSize: "13px",
    fontWeight: "700",
    borderRadius: "4px",
    border: "none",
    backgroundColor: disabled ? theme.appBorderColor : theme.color.secondary,
    color: "#FFFFFF",
    cursor: disabled ? "not-allowed" : "pointer",
    transition: "background-color 0.2s",
    "&:hover": {
      backgroundColor: disabled ? theme.appBorderColor : theme.color.secondaryHot || theme.color.secondary
    }
  })), ThankYouContainer = styled.div(({ theme }) => ({
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    padding: "32px 16px",
    textAlign: "center",
    color: theme.textColor
  })), ThankYouTitle = styled.h3(({ theme }) => ({
    fontSize: "18px",
    fontWeight: "700",
    margin: "0 0 8px 0",
    color: theme.textColor
  })), ThankYouText = styled.p(({ theme }) => ({
    fontSize: "13px",
    margin: 0,
    color: theme.color.mediumdark || "#666666"
  })), SurveyForm = ({
    config,
    isCompleted,
    onSubmit,
    onClose,
    onSkipPermanent
  }) => {
    let storage = useSurveyStorage(config.surveyId), [values, setValues] = useState(() => storage.getDraft()), [errors, setErrors] = useState({}), [isSubmitting, setIsSubmitting] = useState(!1), [isSubmitted, setIsSubmitted] = useState(() => !!isCompleted);
    useEffect(() => {
      isSubmitted || storage.saveDraft(values);
    }, [values, storage, isSubmitted]);
    let handleFieldChange = (fieldId, val) => {
      setValues((prev) => ({ ...prev, [fieldId]: val })), errors[fieldId] && setErrors((prev) => {
        let next = { ...prev };
        return delete next[fieldId], next;
      });
    }, handleCheckboxChange = (fieldId, option, checked) => {
      let current = values[fieldId] || [], next = checked ? [...current, option] : current.filter((item) => item !== option);
      handleFieldChange(fieldId, next);
    }, validate = () => {
      let newErrors = {};
      return config.questions.forEach((question) => {
        if (question.required) {
          let val = values[question.id];
          question.type === "checkbox" ? (!val || val.length === 0) && (newErrors[question.id] = "Please select at least one option.") : question.type === "rating" ? (!val || val === 0) && (newErrors[question.id] = "Please select a rating.") : (!val || typeof val == "string" && val.trim() === "") && (newErrors[question.id] = "This field is required.");
        }
      }), setErrors(newErrors), Object.keys(newErrors).length === 0;
    }, handleSubmit = async (e) => {
      if (e.preventDefault(), !!validate()) {
        setIsSubmitting(!0);
        try {
          await onSubmit(values), storage.clearDraft(), setIsSubmitted(!0);
        } catch (err) {
          console.error("Failed to submit survey:", err), setErrors({ submit: "Failed to submit feedback. Please try again." });
        } finally {
          setIsSubmitting(!1);
        }
      }
    };
    return isSubmitted ? (0, import_jsx_runtime5.jsxs)(ThankYouContainer, { children: [
      (0, import_jsx_runtime5.jsx)(ThankYouTitle, { children: "Thank you!" }),
      (0, import_jsx_runtime5.jsx)(ThankYouText, { children: "Your feedback has been successfully submitted." }),
      (0, import_jsx_runtime5.jsx)(ButtonContainer, { style: { marginTop: "24px", justifyContent: "center" }, children: (0, import_jsx_runtime5.jsx)(SubmitButton, { onClick: onClose, children: "Close" }) })
    ] }) : (0, import_jsx_runtime5.jsxs)(FormContainer, { onSubmit: handleSubmit, children: [
      config.questions.map((question) => {
        let hasError = !!errors[question.id];
        return (0, import_jsx_runtime5.jsxs)(FormGroup, { children: [
          (0, import_jsx_runtime5.jsxs)(Label, { htmlFor: question.id, children: [
            question.label,
            question.required && (0, import_jsx_runtime5.jsx)(RequiredAsterisk, { children: "*" })
          ] }),
          question.type === "rating" && (0, import_jsx_runtime5.jsx)(
            StarRatingInput,
            {
              id: question.id,
              value: values[question.id] || 0,
              onChange: (rating) => handleFieldChange(question.id, rating)
            }
          ),
          question.type === "text" && (0, import_jsx_runtime5.jsx)(
            TextInputField,
            {
              id: question.id,
              placeholder: question.placeholder,
              value: values[question.id] || "",
              onChange: (text) => handleFieldChange(question.id, text)
            }
          ),
          question.type === "textarea" && (0, import_jsx_runtime5.jsx)(
            TextAreaField,
            {
              id: question.id,
              placeholder: question.placeholder,
              value: values[question.id] || "",
              onChange: (text) => handleFieldChange(question.id, text)
            }
          ),
          question.type === "radio" && question.options && (0, import_jsx_runtime5.jsx)(
            RadioGroupInput,
            {
              name: question.id,
              options: question.options,
              value: values[question.id] || "",
              onChange: (option) => handleFieldChange(question.id, option)
            }
          ),
          question.type === "checkbox" && question.options && (0, import_jsx_runtime5.jsx)(
            CheckboxGroupInput,
            {
              options: question.options,
              values: values[question.id] || [],
              onChange: (option, checked) => handleCheckboxChange(question.id, option, checked)
            }
          ),
          hasError && (0, import_jsx_runtime5.jsx)(ErrorText, { children: errors[question.id] })
        ] }, question.id);
      }),
      errors.submit && (0, import_jsx_runtime5.jsx)(ErrorText, { style: { textAlign: "center" }, children: errors.submit }),
      (0, import_jsx_runtime5.jsxs)(ButtonContainer, { children: [
        (0, import_jsx_runtime5.jsx)(
          SubmitButton,
          {
            type: "button",
            style: { background: "transparent", color: "inherit", border: "1px solid currentColor", marginRight: "auto" },
            onClick: onSkipPermanent,
            children: "Don't show again"
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          SubmitButton,
          {
            type: "button",
            style: { background: "transparent", color: "inherit", border: "1px solid currentColor" },
            onClick: onClose,
            children: "Cancel"
          }
        ),
        (0, import_jsx_runtime5.jsx)(SubmitButton, { type: "submit", disabled: isSubmitting, children: isSubmitting ? "Submitting..." : "Submit Feedback" })
      ] })
    ] });
  }, ModalDialog = styled.dialog({
    padding: 0,
    border: "none",
    background: "transparent",
    width: "min(520px, calc(100vw - 32px))",
    maxWidth: "520px",
    maxHeight: "85vh",
    overflow: "visible",
    zIndex: 999999,
    // Ensure it's above everything else in Storybook
    fontFamily: '"Nunito Sans", -apple-system, sans-serif',
    whiteSpace: "normal",
    "&::backdrop": {
      backgroundColor: "rgba(0, 0, 0, 0.5)",
      backdropFilter: "blur(3px)"
    }
  }), ModalCard = styled.div(({ theme }) => ({
    backgroundColor: theme.background.content,
    borderRadius: "8px",
    border: `1px solid ${theme.appBorderColor}`,
    boxShadow: "0 12px 36px rgba(0, 0, 0, 0.25)",
    width: "100%",
    maxHeight: "85vh",
    display: "flex",
    flexDirection: "column",
    overflow: "hidden",
    animation: "sbSurveyFadeIn 0.25s cubic-bezier(0.16, 1, 0.3, 1) both",
    "@keyframes sbSurveyFadeIn": {
      from: {
        opacity: 0,
        transform: "translateY(12px) scale(0.97)"
      },
      to: {
        opacity: 1,
        transform: "translateY(0) scale(1)"
      }
    }
  })), ModalHeader = styled.div(({ theme }) => ({
    padding: "20px 24px 16px 24px",
    borderBottom: `1px solid ${theme.appBorderColor}`,
    display: "flex",
    flexDirection: "column",
    gap: "4px",
    minWidth: 0,
    overflow: "hidden"
  })), HeaderTopRow = styled.div({
    display: "flex",
    justifyContent: "space-between",
    alignItems: "flex-start",
    gap: "12px",
    minWidth: 0
  }), CloseIconButton = styled.button(({ theme }) => ({
    flexShrink: 0,
    background: "none",
    border: "none",
    cursor: "pointer",
    padding: "4px",
    color: theme.color.mediumdark || "#888888",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: "4px",
    transition: "background-color 0.2s, color 0.2s",
    "&:hover": {
      backgroundColor: theme.appBorderColor,
      color: theme.textColor
    }
  })), CloseIcon2 = () => (0, import_jsx_runtime6.jsxs)("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: [
    (0, import_jsx_runtime6.jsx)("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
    (0, import_jsx_runtime6.jsx)("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
  ] }), ModalTitle = styled.h2(({ theme }) => ({
    fontSize: "18px",
    fontWeight: "800",
    margin: 0,
    color: theme.textColor,
    minWidth: 0,
    overflowWrap: "break-word",
    wordBreak: "break-all"
  })), ModalDescription = styled.p(({ theme }) => ({
    fontSize: "13px",
    margin: 0,
    lineHeight: "18px",
    color: theme.color.mediumdark || "#666666",
    minWidth: 0,
    whiteSpace: "normal",
    overflowWrap: "break-word",
    wordBreak: "break-all"
  })), ModalBody = styled.div({
    padding: "20px 24px 24px 24px",
    overflowY: "auto",
    flex: 1
  }), SurveyModal = ({
    isOpen,
    config,
    isCompleted,
    onSubmit,
    onClose,
    onSkipPermanent
  }) => {
    let dialogRef = react_default.useRef(null), titleId = react_default.useId(), descriptionId = react_default.useId();
    return react_default.useEffect(() => {
      let dialog = dialogRef.current;
      if (!dialog)
        return;
      let handleCancel = (event) => {
        event.preventDefault(), onClose();
      }, handleBackdropClick = (event) => {
        event.target === dialog && onClose();
      };
      return dialog.addEventListener("cancel", handleCancel), dialog.addEventListener("click", handleBackdropClick), dialog.open || dialog.showModal(), () => {
        dialog.removeEventListener("cancel", handleCancel), dialog.removeEventListener("click", handleBackdropClick), dialog.open && dialog.close();
      };
    }, [onClose]), isOpen ? (0, import_jsx_runtime6.jsx)(
      ModalDialog,
      {
        ref: dialogRef,
        "aria-labelledby": titleId,
        "aria-describedby": config.description ? descriptionId : void 0,
        children: (0, import_jsx_runtime6.jsxs)(ModalCard, { children: [
          (0, import_jsx_runtime6.jsxs)(ModalHeader, { children: [
            (0, import_jsx_runtime6.jsxs)(HeaderTopRow, { children: [
              (0, import_jsx_runtime6.jsx)(ModalTitle, { id: titleId, children: config.title }),
              (0, import_jsx_runtime6.jsx)(CloseIconButton, { onClick: onClose, "aria-label": "Close survey", autoFocus: !0, children: (0, import_jsx_runtime6.jsx)(CloseIcon2, {}) })
            ] }),
            config.description && (0, import_jsx_runtime6.jsx)(ModalDescription, { id: descriptionId, children: config.description })
          ] }),
          (0, import_jsx_runtime6.jsx)(ModalBody, { children: (0, import_jsx_runtime6.jsx)(
            SurveyForm,
            {
              config,
              isCompleted,
              onSubmit,
              onClose,
              onSkipPermanent
            },
            config.surveyId
          ) })
        ] })
      }
    ) : null;
  }, useSurveyLifecycle = ({
    config,
    isCompleted,
    isSkippedPermanently,
    dismissedAt,
    impressionCount,
    incrementImpressions,
    api
  }) => {
    let [isOpen, setIsOpen] = useState(!1), [navCount, setNavCount] = useState(0), timerRef = useRef(null), triggerOptions = config.trigger || {}, isExpired = triggerOptions.expiresAt ? /* @__PURE__ */ new Date() > new Date(triggerOptions.expiresAt) : !1, isGloballyDisabled = config.enabled === !1, isMaxImpressionsReached = !!triggerOptions.maxImpressions && impressionCount >= triggerOptions.maxImpressions, isSnoozed = !1;
    if (dismissedAt && triggerOptions.coolDownDays) {
      let coolDownMs = triggerOptions.coolDownDays * 24 * 60 * 60 * 1e3;
      Date.now() - dismissedAt < coolDownMs && (isSnoozed = !0);
    }
    let shouldBlockAutoPopup = isCompleted || isSkippedPermanently || isExpired || isGloballyDisabled || isMaxImpressionsReached || isSnoozed;
    return useEffect(() => {
      if (shouldBlockAutoPopup || isOpen || !config.questions || config.questions.length === 0)
        return;
      let delay = triggerOptions.delayMs !== void 0 ? triggerOptions.delayMs : 5e3;
      return delay > 0 && (timerRef.current = setTimeout(() => {
        incrementImpressions(), setIsOpen(!0);
      }, delay)), () => {
        timerRef.current && clearTimeout(timerRef.current);
      };
    }, [
      shouldBlockAutoPopup,
      config.questions,
      triggerOptions.delayMs,
      isOpen,
      incrementImpressions
    ]), useEffect(() => {
      let handleStoryChanged = () => {
        setNavCount((prev) => {
          let nextCount = prev + 1, requiredNavs = triggerOptions.storyCount !== void 0 ? triggerOptions.storyCount : 3;
          return !shouldBlockAutoPopup && !isOpen && requiredNavs > 0 && nextCount >= requiredNavs && (incrementImpressions(), setIsOpen(!0)), nextCount;
        });
      };
      return api.on("storyChanged", handleStoryChanged), () => {
        api.off("storyChanged", handleStoryChanged);
      };
    }, [api, shouldBlockAutoPopup, triggerOptions.storyCount, isOpen, incrementImpressions]), useEffect(() => {
      let handleKeyDown = (e) => {
        e.altKey && e.shiftKey && e.key.toLowerCase() === "s" && (e.preventDefault(), setIsOpen((prev) => !prev));
      };
      return window.addEventListener("keydown", handleKeyDown), () => {
        window.removeEventListener("keydown", handleKeyDown);
      };
    }, []), {
      isOpen,
      setIsOpen,
      shouldBlockAutoPopup,
      navCount
    };
  }, submitFeedbackWebhook = async (config, responses) => {
    let payload = {
      surveyId: config.surveyId,
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      responses
    };
    if (!config.webhookUrl)
      return payload;
    let headers = {
      "Content-Type": "application/json",
      ...config.webhookHeaders || {}
    };
    try {
      let res = await fetch(config.webhookUrl, {
        method: "POST",
        headers,
        body: JSON.stringify(payload)
      });
      if (!res.ok)
        throw new Error(`Server responded with status ${res.status}: ${res.statusText}`);
    } catch (err) {
      throw console.error("[Feedback Survey Addon] Webhook submission failed:", err), err;
    }
    return payload;
  }, ADDON_ID = "storybook-feedback-survey", TOOL_ID = `${ADDON_ID}/tool`, IconWrapper = styled.div({
    position: "relative",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center"
  }), NotificationDot = styled.div(({ theme }) => ({
    position: "absolute",
    top: "2px",
    right: "2px",
    width: "6px",
    height: "6px",
    borderRadius: "50%",
    backgroundColor: theme.color.negative || "#FF4400"
  })), SurveyManager = ({ api }) => {
    let buildTimeOptions = typeof STORYBOOK_FEEDBACK_SURVEY_OPTIONS < "u" ? STORYBOOK_FEEDBACK_SURVEY_OPTIONS : {}, parameterOptions = useParameter("feedbackSurvey", null), config = {
      surveyId: "default-survey-v1",
      title: "Feedback Survey",
      description: "Help us improve by sharing your thoughts!",
      questions: [],
      ...buildTimeOptions,
      ...parameterOptions
    }, storage = useSurveyStorage(config.surveyId), { isOpen, setIsOpen, shouldBlockAutoPopup } = useSurveyLifecycle({
      config,
      isCompleted: storage.isCompleted,
      isSkippedPermanently: storage.isSkippedPermanently,
      dismissedAt: storage.dismissedAt,
      impressionCount: storage.impressionCount,
      incrementImpressions: storage.incrementImpressions,
      api
    }), handleOpen = () => {
      setIsOpen(!0);
    }, handleClose = () => {
      setIsOpen(!1), storage.isCompleted || storage.setDismissed(Date.now());
    }, handleSkipPermanent = () => {
      setIsOpen(!1), storage.setSkippedPermanently();
    }, handleSubmit = async (responses) => {
      let payload = await submitFeedbackWebhook(config, responses);
      api.getChannel().emit("feedback-survey/submitted", payload), console.log("[Feedback Survey Addon] Feedback submitted successfully:", payload), storage.setCompleted();
    };
    if (!config.questions || config.questions.length === 0)
      return null;
    let showNotification = !storage.isCompleted && !storage.isSkippedPermanently;
    return (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, { children: [
      (0, import_jsx_runtime7.jsx)(
        IconButton,
        {
          title: "Share your feedback",
          onClick: handleOpen,
          children: (0, import_jsx_runtime7.jsxs)(IconWrapper, { children: [
            (0, import_jsx_runtime7.jsx)(SupportIcon, {}),
            showNotification && (0, import_jsx_runtime7.jsx)(NotificationDot, {})
          ] })
        },
        "feedback-survey-toolbar-button"
      ),
      (0, import_jsx_runtime7.jsx)(
        SurveyModal,
        {
          isOpen,
          config,
          isCompleted: storage.isCompleted,
          onSubmit: handleSubmit,
          onClose: handleClose,
          onSkipPermanent: handleSkipPermanent
        },
        config.surveyId
      )
    ] });
  };
  addons.register(ADDON_ID, (api) => {
    addons.add(TOOL_ID, {
      title: "Feedback Survey",
      type: types.TOOL,
      match: ({ viewMode }) => viewMode === "story",
      render: () => (0, import_jsx_runtime7.jsx)(SurveyManager, { api })
    });
  });
})();
}catch(e){ console.error("[Storybook] One of your manager-entries failed: " + import.meta.url, e); }
